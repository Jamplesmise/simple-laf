# MongoDB 连接 (必需)
MONGO_URL=mongodb://localhost:27017/simple-ide

# JWT 密钥 (必需，生产环境请使用随机字符串)
JWT_SECRET=change-this-to-a-random-string

# JWT 过期时间 (可选，默认 7d)
JWT_EXPIRES_IN=7d

# 开发调试 Token (可选)
DEVELOP_TOKEN=simple-ide-develop-token

# 服务端口 (可选，默认 3000)
PORT=3000
