# Sprint 10: Chat 基础增强 - 任务清单

## 任务概览

| 任务 | 子模块 | 轨道 | 优先级 | 预估 | 依赖 | 状态 |
|------|--------|------|-------|------|------|------|
| 10.1.1 状态类型定义 | 状态可视化 | 后端 | P0 | 1h | 无 | ✅ 完成 |
| 10.1.2 SSE 状态推送增强 | 状态可视化 | 后端 | P0 | 2h | 10.1.1 | ✅ 完成 |
| 10.1.3 状态面板组件 | 状态可视化 | 前端 | P0 | 3h | 10.1.2 | ✅ 完成 |
| 10.2.1 消息编辑 API | 消息操作 | 后端 | P0 | 2h | 无 | ✅ 完成 |
| 10.2.2 分支对话 API | 消息操作 | 后端 | P0 | 2h | 无 | ✅ 完成 |
| 10.2.3 消息操作组件 | 消息操作 | 前端 | P0 | 3h | 10.2.1-2 | ✅ 完成 |
| 10.3.1 上下文统计 API | 上下文管理 | 后端 | P0 | 2h | 无 | ✅ 完成 |
| 10.3.2 上下文压缩服务 | 上下文管理 | 后端 | P1 | 3h | 10.3.1 | ✅ 完成 |
| 10.3.3 上下文管理组件 | 上下文管理 | 前端 | P0 | 4h | 10.3.1-2 | ✅ 完成 |

---

## 10.1 AI 状态可视化

### 10.1.1 状态类型定义

**文件**：`src/server/services/ai/types.ts`

```typescript
// AI 状态类型
type AIStatus =
  | { type: 'idle' }
  | { type: 'thinking', content?: string }
  | { type: 'calling_tool', tool: string, params: Record<string, unknown> }
  | { type: 'executing', operation: string, progress?: number }
  | { type: 'streaming', tokens: number }
  | { type: 'waiting_confirm', operations: Operation[] }
  | { type: 'error', message: string }

interface AIStatusUpdate {
  status: AIStatus
  timestamp: number
  tokenUsage?: {
    input: number
    output: number
    cost: number
  }
}
```

**验收**：
- [x] 类型定义完整
- [x] 前后端共享类型

---

### 10.1.2 SSE 状态推送增强

**文件**：`src/server/services/ai/index.ts`

**具体步骤**：

- [x] 在工具调用前发送 `tool_call` 事件
- [x] 在工具调用后发送 `tool_result` 事件
- [x] 在思考阶段发送 `thinking` 事件
- [x] 在流式输出时发送 `token_usage` 事件

**代码示例**：

```typescript
// 工具调用前
sendSSE(res, {
  type: 'tool_call',
  data: { tool: toolName, params }
})

// 工具调用后
sendSSE(res, {
  type: 'tool_result',
  data: { tool: toolName, result, success: true }
})

// Token 使用
sendSSE(res, {
  type: 'token_usage',
  data: { input: inputTokens, output: outputTokens, cost: calculateCost() }
})
```

**验收**：
- [x] 工具调用前后都有事件推送
- [x] Token 使用实时更新
- [x] 兼容现有 SSE 消息流

---

### 10.1.3 状态面板组件

**文件**：`src/client/components/AI/StatusPanel/index.tsx`

**具体步骤**：

- [x] 创建 `StatusPanel` 主组件
- [x] 创建 `ToolCallCard` 显示工具调用
- [x] 创建 `ThinkingView` 显示思考过程（可折叠）
- [x] 创建 `TokenUsageBar` 显示 Token 消耗

**UI 设计**：

```
┌───────────────────────────────────────────────────────┐
│ 🔄 正在分析代码结构...                                  │
│ ████████████░░░░░░░░ 60%                              │
├───────────────────────────────────────────────────────┤
│ 🔧 调用工具: analyze_code                              │
│    参数: { functionId: "xxx", depth: 3 }             │
│    状态: 执行中 ⏳                                     │
├───────────────────────────────────────────────────────┤
│ 💭 思考中... [展开 ▼]                                  │
├───────────────────────────────────────────────────────┤
│ 📊 本次对话: 输入 1,234 | 输出 567 | $0.0123          │
└───────────────────────────────────────────────────────┘
```

**验收**：
- [x] 实时显示 AI 状态
- [x] 工具调用信息清晰展示
- [x] 思考过程可展开/折叠
- [x] Token 消耗实时更新

---

## 10.2 消息编辑与分支 ✅ 已完成

### 10.2.1 消息编辑 API ✅

**文件**：`src/server/routes/ai.ts`, `src/server/services/ai/conversation.ts`

**API**：`PATCH /api/ai/messages/:id`

```typescript
// 请求
{
  content: string  // 新的消息内容
}

// 响应
{
  success: true,
  data: {
    messageId: string,
    version: number,
    regenerating: boolean,  // 是否触发重新生成
    deletedCount: number    // 删除的后续消息数
  }
}
```

**逻辑**：
1. 更新消息内容
2. 递增版本号
3. 如果是用户消息，删除后续 AI 回复
4. 触发重新生成

**验收**：
- [x] 消息内容更新成功
- [x] 版本号递增
- [x] 后续回复正确删除
- [x] 触发重新生成

---

### 10.2.2 分支对话 API ✅

**文件**：`src/server/routes/ai.ts`, `src/server/services/ai/conversation.ts`

**API**：
- `POST /api/ai/messages/:id/branch` - 创建分支
- `POST /api/ai/messages/:id/feedback` - 消息反馈

```typescript
// 分支请求
{
  newContent?: string  // 可选的新消息内容
}

// 分支响应
{
  success: true,
  data: {
    branchConversationId: string,  // 新分支对话 ID
    parentMessageId: string,
    conversation: AIConversation,
    messageCount: number
  }
}

// 反馈请求
{
  feedback: 'like' | 'dislike' | null,
  note?: string
}
```

**逻辑**：
1. 复制消息到指定位置
2. 创建新的对话分支
3. 设置 `parentMessageId` 关联

**验收**：
- [x] 分支对话创建成功
- [x] 消息历史正确复制
- [x] 父消息关联正确
- [x] 反馈 API 正常工作

---

### 10.2.3 消息操作组件 ✅

**文件**：`src/client/components/AIConversationDialog/MessageActions/`

**实现文件**：
- `index.tsx` - 消息操作按钮组
- `EditDialog.tsx` - 编辑弹窗
- `styles.module.css` - 样式文件

**具体步骤**：

- [x] 创建消息操作按钮组（编辑/分支/复制/反馈）
- [x] 创建 `EditDialog` 编辑弹窗
- [x] 分支按钮集成在主组件中
- [x] 反馈按钮集成在主组件中

**UI 设计**：

```
┌─────────────────────────────────────────────────────┐
│ 用户消息内容...                                       │
│                                      [✏️] [🔀] [📋] │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ AI 回复内容...                                        │
│                              [👍] [👎] [🔀] [📋]    │
└─────────────────────────────────────────────────────┘
```

**验收**：
- [x] 用户消息可编辑
- [x] 消息可分支
- [x] 一键复制功能正常
- [x] 反馈按钮可点击
- [x] hover 时显示操作按钮

---

## 10.3 上下文可视化管理 ✅ 已完成

### 10.3.1 上下文统计 API ✅

**文件**：`src/server/services/ai/context/calculator.ts`, `src/server/routes/ai.ts`

**API**：`GET /api/ai/conversations/:id/context`

```typescript
// 响应
{
  success: true,
  data: {
    usage: {
      used: number,      // 已使用 tokens
      total: number,     // 总限制
      percentage: number // 百分比
    },
    items: [
      {
        id: string,
        type: 'system' | 'message' | 'code' | 'tool_result',
        source: string,
        tokens: number,
        removable: boolean,
        compressible: boolean
      }
    ]
  }
}
```

**验收**：
- [x] 正确计算 Token 使用量
- [x] 分类统计准确
- [x] 可删除/可压缩标记正确

---

### 10.3.2 上下文压缩服务 ✅

**文件**：`src/server/services/ai/context/compressor.ts`, `src/server/routes/ai.ts`

**API**：`POST /api/ai/conversations/:id/compress`

```typescript
// 请求
{
  mode: 'smart' | 'manual',
  targetPercentage?: number,  // smart 模式目标
  itemIds?: string[]          // manual 模式指定项
}

// 响应
{
  success: true,
  data: {
    before: number,
    after: number,
    actions: string[]
  }
}
```

**压缩策略**：
1. 压缩工具结果（保留摘要）
2. 压缩早期对话（保留关键信息）
3. 代码转为骨架（保留函数签名）
4. 删除低重要性内容

**验收**：
- [x] 智能压缩可降至目标百分比
- [x] 压缩后信息保留完整
- [x] 手动压缩指定项成功

---

### 10.3.3 上下文管理组件 ✅

**文件**：`src/client/components/AI/ContextManager/`

**实现文件**：
- `index.tsx` - 主组件
- `UsageBar.tsx` - 使用量指示器
- `ContextList.tsx` - 上下文列表

**具体步骤**：

- [x] 创建 `UsageBar` 使用量指示器
- [x] 创建 `ContextList` 上下文列表（分类展示）
- [x] 创建压缩和删除功能（集成在主组件中）
- [x] 70% 阈值显示蓝色提示

**UI 设计**：

```
┌─────────────────────────────────────────────────────┐
│  ████████████████████████░░░░░░░░░░  72%            │
│                              [查看上下文]           │
│  ⚠️ 上下文已超过 70%，建议清理或压缩                  │
└─────────────────────────────────────────────────────┘

点击后展开：
┌─────────────────────────────────────────────────────┐
│  📊 上下文内容 (28,450 / 40,000 tokens)              │
│  ─────────────────────────────────────────────────  │
│  📁 系统提示词                    2,100 tokens       │
│  💬 对话历史                     12,350 tokens       │
│     ├─ □ [消息 1] 用户: ...        320 tokens  [✕]  │
│     └─ ...                                          │
│  📄 引用的函数代码                 8,500 tokens       │
│  🔧 工具调用结果                   5,500 tokens       │
│  ─────────────────────────────────────────────────  │
│  [删除选中] [压缩选中] [🔮 智能压缩]                  │
└─────────────────────────────────────────────────────┘
```

**验收**：
- [x] 使用量百分比显示准确
- [x] 70% 阈值提示正常
- [x] 分类展示清晰
- [x] 删除和压缩功能正常

---

## 并行开发说明

```
┌────────────────────────────────────────────────────────────┐
│                     Sprint 10 并行开发图                     │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  后端开发者 A:                                              │
│  ├─ 10.1.1 状态类型定义 (1h)                               │
│  └─ 10.1.2 SSE 状态推送增强 (2h)                           │
│                                                            │
│  后端开发者 B:                                              │
│  ├─ 10.2.1 消息编辑 API (2h)                               │
│  └─ 10.2.2 分支对话 API (2h)                               │
│                                                            │
│  后端开发者 C:                                              │
│  ├─ 10.3.1 上下文统计 API (2h)                             │
│  └─ 10.3.2 上下文压缩服务 (3h)                             │
│                                                            │
│  前端开发者:                                                │
│  ├─ 10.1.3 状态面板组件 (3h) → 依赖 10.1.2                 │
│  ├─ 10.2.3 消息操作组件 (3h) → 依赖 10.2.1-2               │
│  └─ 10.3.3 上下文管理组件 (4h) → 依赖 10.3.1-2             │
│                                                            │
│  时间线:                                                    │
│  Day 1: 后端任务全部完成（可并行）                           │
│  Day 2: 前端任务全部完成                                    │
│  Day 3: 集成测试                                           │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 测试清单

- [ ] 单元测试：状态类型序列化/反序列化
- [ ] 单元测试：Token 计算准确性
- [ ] 集成测试：消息编辑重新生成流程
- [ ] 集成测试：分支对话创建和切换
- [ ] E2E 测试：上下文管理完整流程
