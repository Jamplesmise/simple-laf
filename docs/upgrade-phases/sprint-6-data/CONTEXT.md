# Sprint 6: 数据管理 - MongoDB 集合 + S3 对象存储

## 阶段目标

为 Simple IDE 添加数据管理功能，包括 MongoDB 集合管理和 S3 对象存储管理，让用户可以在 IDE 内直接操作数据。

## 功能范围

### 核心功能

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 集合列表 | 查看数据库所有集合 | P0 |
| 文档 CRUD | 增删查改集合内文档 | P0 |
| 文档查询 | MongoDB 查询语法支持 | P0 |
| 索引管理 | 查看/创建/删除索引 | P1 |
| S3 连接配置 | 配置 S3/MinIO 连接信息 | P1 |
| 文件浏览 | 浏览存储桶和文件 | P1 |
| 文件上传/下载 | 上传下载文件 | P1 |

### 增强功能

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 集合创建/删除 | 管理集合生命周期 | P2 |
| 文档导入/导出 | JSON/CSV 批量操作 | P2 |
| 存储桶管理 | 创建/删除存储桶 | P2 |
| 文件预览 | 图片/文本/JSON 在线预览 | P2 |
| 拖拽上传 | 拖拽文件直接上传 | P2 |

### 不包含

- 数据库备份/恢复
- 数据迁移工具
- 多数据库连接管理
- S3 生命周期策略配置

## 技术选型

| 组件 | 选择 | 理由 |
|------|------|------|
| MongoDB 操作 | 原生驱动 | 项目已使用，无需额外依赖 |
| S3 客户端 | @aws-sdk/client-s3 | 官方 SDK，兼容 MinIO |
| 文件上传 | multipart/form-data | 标准方式，支持大文件 |
| JSON 编辑 | Monaco Editor | 复用现有编辑器 |

## 数据模型

### s3_config 集合 (新增)

```typescript
interface S3Config {
  _id: ObjectId;
  userId: ObjectId;
  endpoint: string;          // S3/MinIO 端点
  accessKeyId: string;       // Access Key (加密存储)
  secretAccessKey: string;   // Secret Key (加密存储)
  region: string;            // 区域 (默认 us-east-1)
  bucket: string;            // 默认存储桶
  forcePathStyle: boolean;   // MinIO 需要设为 true
  createdAt: Date;
  updatedAt: Date;
}
```

## API 设计

### MongoDB 集合管理

```
GET    /api/database/collections            # 获取集合列表
POST   /api/database/collections            # 创建集合
DELETE /api/database/collections/:name      # 删除集合

GET    /api/database/collections/:name/documents     # 查询文档 (支持分页、查询条件)
POST   /api/database/collections/:name/documents     # 插入文档
PUT    /api/database/collections/:name/documents/:id # 更新文档
DELETE /api/database/collections/:name/documents/:id # 删除文档

GET    /api/database/collections/:name/indexes       # 获取索引列表
POST   /api/database/collections/:name/indexes       # 创建索引
DELETE /api/database/collections/:name/indexes/:name # 删除索引

GET    /api/database/collections/:name/stats         # 集合统计信息
```

### S3 对象存储

```
GET    /api/storage/config              # 获取 S3 配置
PUT    /api/storage/config              # 保存 S3 配置
POST   /api/storage/config/test         # 测试连接

GET    /api/storage/buckets             # 获取存储桶列表
POST   /api/storage/buckets             # 创建存储桶
DELETE /api/storage/buckets/:name       # 删除存储桶

GET    /api/storage/objects             # 列出对象 (支持前缀、分页)
POST   /api/storage/objects/upload      # 上传文件
GET    /api/storage/objects/download    # 下载文件
DELETE /api/storage/objects             # 删除对象
POST   /api/storage/objects/folder      # 创建文件夹

GET    /api/storage/objects/presigned   # 获取预签名 URL
```

## 目录结构变更

```
packages/server/src/
├── routes/
│   ├── database.ts              # 新增：数据库管理路由
│   └── storage.ts               # 新增：对象存储路由
├── services/
│   ├── database.ts              # 新增：MongoDB 操作服务
│   └── storage.ts               # 新增：S3 操作服务
└── models/
    └── s3Config.ts              # 新增：S3 配置模型

packages/web/src/
├── api/
│   ├── database.ts              # 新增：数据库 API
│   └── storage.ts               # 新增：存储 API
├── stores/
│   └── dataStore.ts             # 新增：数据管理状态
├── components/
│   ├── Database/
│   │   ├── DatabasePanel.tsx    # 数据库管理主面板
│   │   ├── CollectionList.tsx   # 集合列表
│   │   ├── DocumentList.tsx     # 文档列表
│   │   ├── DocumentEditor.tsx   # 文档编辑器
│   │   ├── QueryBuilder.tsx     # 查询构建器
│   │   └── IndexManager.tsx     # 索引管理
│   └── Storage/
│       ├── StoragePanel.tsx     # 存储管理主面板
│       ├── BucketList.tsx       # 存储桶列表
│       ├── FileList.tsx         # 文件列表
│       ├── FileUploader.tsx     # 文件上传组件
│       └── FilePreview.tsx      # 文件预览
└── pages/
    └── IDE/
        └── index.tsx            # 修改：添加数据管理入口
```

## 前端交互设计

### 页面布局

数据管理采用独立页面，三栏布局：

```
┌─────────────────────────────────────────────────────────────┐
│  Header (切换: 云函数 | 数据库 | 存储)                        │
├────────────┬──────────────────────────┬─────────────────────┤
│            │                          │                     │
│ 集合列表    │    文档列表 / 文件列表    │   文档编辑器        │
│ 或          │    (表格视图)            │   或                │
│ 存储桶列表  │                          │   文件预览          │
│            │                          │                     │
├────────────┼──────────────────────────┴─────────────────────┤
│ [+] 新建   │  查询栏: [{ "name": "..." }]  [查询]  [刷新]    │
└────────────┴────────────────────────────────────────────────┘
```

### 数据库管理界面

```
┌─────────────────────────────────────────────────────────────┐
│  数据库                                              [设置] │
├────────────┬────────────────────────────────────────────────┤
│            │  查询: [                              ] [执行]  │
│  集合列表   ├────────────────────────────────────────────────┤
│            │  _id          │ name    │ email    │ createdAt │
│  ▸ users   │  673a...      │ Alice   │ a@b.com  │ 2024-12.. │
│  ▸ functions│  673b...      │ Bob     │ b@c.com  │ 2024-12.. │
│  ▸ folders │  673c...      │ Carol   │ c@d.com  │ 2024-12.. │
│            │                                                │
│ [+] 新建集合├────────────────────────────────────────────────┤
│            │  显示 1-20 / 共 156 条     [<] 1 2 3 ... [>]   │
└────────────┴────────────────────────────────────────────────┘
```

### 存储管理界面

```
┌─────────────────────────────────────────────────────────────┐
│  对象存储                                       [配置] [刷新]│
├────────────┬────────────────────────────────────────────────┤
│            │  路径: / images / avatars /           [上传]   │
│  存储桶     ├────────────────────────────────────────────────┤
│            │  名称           │ 大小    │ 修改时间           │
│  ▸ uploads │  📁 thumbnails │ -       │ 2024-12-18        │
│  ▸ avatars │  🖼 avatar1.jpg │ 125 KB  │ 2024-12-18        │
│  ▸ exports │  🖼 avatar2.png │ 89 KB   │ 2024-12-17        │
│            │  📄 config.json │ 2 KB    │ 2024-12-16        │
│ [+] 新建桶  │                                                │
│            ├────────────────────────────────────────────────┤
│            │  已选择 2 个文件              [下载] [删除]     │
└────────────┴────────────────────────────────────────────────┘
```

## 验收标准

### P0 核心功能

- [ ] 显示项目 MongoDB 所有集合
- [ ] 查询集合内文档，支持 MongoDB 查询语法
- [ ] 新增/编辑/删除单个文档
- [ ] 文档内容使用 JSON 编辑器展示

### P1 功能

- [ ] 查看/创建/删除索引
- [ ] S3 连接配置保存和测试
- [ ] 浏览存储桶内文件列表
- [ ] 上传/下载文件

### P2 功能

- [ ] 创建/删除集合
- [ ] 文档批量导入/导出
- [ ] 创建/删除存储桶
- [ ] 图片/文本文件预览
- [ ] 拖拽上传

## 依赖

- Sprint 1-5 基础功能完成
- 需要安装 `@aws-sdk/client-s3` 包

## 安全考虑

### 数据库操作

1. **集合白名单** - 可配置哪些集合允许在 UI 操作
2. **系统集合保护** - `users`, `sessions` 等敏感集合只读或隐藏
3. **操作审计** - 记录所有数据变更操作
4. **查询限制** - 限制单次查询返回数量，防止拉取全表

### S3 操作

1. **密钥加密** - Access Key 和 Secret Key 加密存储
2. **文件大小限制** - 限制单文件上传大小
3. **文件类型检查** - 可配置允许的文件类型
4. **预签名 URL** - 设置合理过期时间

## 技术实现要点

### MongoDB 操作

```typescript
// 使用项目已有的 db 连接
import { db } from '../db.js'

// 列出集合
const collections = await db.listCollections().toArray()

// 查询文档
const docs = await db.collection(name)
  .find(query)
  .skip(skip)
  .limit(limit)
  .toArray()

// 创建索引
await db.collection(name).createIndex(keys, options)
```

### S3 操作

```typescript
import { S3Client, ListBucketsCommand, PutObjectCommand } from '@aws-sdk/client-s3'

const client = new S3Client({
  endpoint: config.endpoint,
  region: config.region,
  credentials: {
    accessKeyId: config.accessKeyId,
    secretAccessKey: config.secretAccessKey,
  },
  forcePathStyle: config.forcePathStyle, // MinIO 需要
})

// 列出存储桶
const { Buckets } = await client.send(new ListBucketsCommand({}))

// 上传文件
await client.send(new PutObjectCommand({
  Bucket: bucket,
  Key: key,
  Body: buffer,
  ContentType: contentType,
}))
```

## 工作量估算

| 模块 | 后端 | 前端 | 总计 |
|------|------|------|------|
| MongoDB 集合管理 | 0.5 天 | 1.5 天 | 2 天 |
| S3 对象存储 | 1 天 | 2 天 | 3 天 |
| **合计** | 1.5 天 | 3.5 天 | **5 天** |

## 里程碑

| 里程碑 | 验收标准 |
|--------|---------|
| M1 | 集合列表显示正常 |
| M2 | 文档 CRUD 功能完整 |
| M3 | MongoDB 查询语法支持 |
| M4 | 索引管理功能可用 |
| M5 | S3 配置保存和连接测试 |
| M6 | 文件浏览和上传下载 |
| M7 | 文件预览功能 |
